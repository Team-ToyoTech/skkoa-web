# 문자열

문자열(string)은 글자들의 모음이다.

## 현재 지원

문자열 리터럴 출력:

```koa
시작
    출력 "안녕하세요"
끝
```

문자열 변수:

```koa
변수 name: 문자열 = "SKKOA"
출력 name
```

문자열 결합:

```koa
출력 "Hello, " + name
```

문자열 길이, 부분 문자열, 비교 함수는 표준 라이브러리(standard library) 확장으로 다룰 예정이다.
