# 제어 흐름

제어 흐름은 코드가 실행되는 순서입니다.

## 만약

조건이 맞을 때만 실행합니다.

```koa
시작
    변수 score: 정수 = 85

    만약 score >= 80 이면
        출력 "통과"
    아니면
        출력 "실패"
    끝
끝
```

## 아니면만약

조건을 여러 개 확인할 수 있습니다.

```koa
만약 score >= 90 이면
    출력 "A"
아니면만약 score >= 80 이면
    출력 "B"
아니면
    출력 "C"
끝
```

## 동안

조건이 맞는 동안 반복합니다.

```koa
시작
    변수 i: 정수 = 0

    동안 i < 5 반복
        출력 i
        i = i + 1
    끝
끝
```

## 반복

정해진 범위만큼 반복합니다.

```koa
시작
    반복 i: 1부터 3까지
        출력 i
    끝
끝
```

## 중단과 계속

`중단`은 반복문을 끝내고, `계속`은 이번 반복의 남은 코드를 건너뜁니다.

```koa
시작
    반복 i: 1부터 6까지
        만약 i == 2 이면
            계속
        끝

        만약 i == 5 이면
            중단
        끝

        출력 i
    끝
끝
```
